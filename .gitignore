# scratch / local
.remember/
scratchpad/
__pycache__/
*.pyc
.env
.venv/
venv/
.DS_Store
${HOME}
.claude/
.serena/
.DS_Store
**/.DS_Store

# --- appended for phase 1 (vector store + model cache) ---
# Chroma persistence directory (sqlite3 + HNSW index binaries)
.chroma/
chroma/
chroma_db/
data/chroma/
*.sqlite3

# Local embedding model cache — BAAI/bge-m3 weights, ~2.3GB
.cache/
models/
.sentence_transformers_cache/

# Eval harness run artifacts (keep the committed scorecard, drop raw runs)
eval/runs/

# Generated SQLite databases — rebuilt deterministically by data/seed_records.py
# and by scripts/e2e_demo_audit.py. Tracked binaries churned on every run.
data/*.db
